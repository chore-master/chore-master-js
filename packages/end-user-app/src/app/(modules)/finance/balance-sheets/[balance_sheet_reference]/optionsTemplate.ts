import { Options } from 'highcharts/highcharts'

export default {
  chart: {
    type: 'pie',
    animation: false,
  },
  plotOptions: {
    pie: {
      animation: false,
    },
  },
} as unknown as Options
